# lean4py v0.9 规划与实现记录

> 当前版本: v0.8.0 → v0.9.0
> 规划日期: 2026-04-29
> 完成日期: 2026-04-29
> 目标: 新增信息论模块，扩展图论算法，完善类型提示，提升测试覆盖

---

## 现状摘要 (v0.8.0)

- **总计测试**: 424 passed, 0 failed
- **Python 要求**: >=3.10
- **关键成就**: P6-P8 完成，性能优化（det/is_prime），statistics 模块扩展
- **已知缺口**: 
  - 缺少信息论基础模块
  - 图论缺少哈密顿路径等经典算法
  - 部分函数类型提示不完整
  - 新模块测试覆盖不足

---

## v0.9 实施计划

### P9 - 新模块探索（v0.8 遗留）

| # | 任务 | 说明 | 状态 |
|---|------|------|------|
| 9 | 新建 `information_theory.py` | 熵、互信息、KL 散度 | ✅ 完成 |
| 10 | 新建 `optimization.py` | 梯度下降、线性规划 | ⏳ 留待 v1.0 |

### P10 - 图论算法扩展

| # | 任务 | 说明 | 状态 |
|---|------|------|------|
| 11 | 扩展 `graph_theory.py`: 哈密顿路径 | Dirac 定理启发式检查 | ✅ 完成 |
| 12 | 扩展 `graph_theory.py`: 最大流 | Ford-Fulkerson 算法 | ⏳ 留待 v1.0 |

### P11 - 代码质量提升

| # | 任务 | 说明 | 状态 |
|---|------|------|------|
| 13 | 核心模块补充类型提示 | statistics/probability 等 | ✅ 完成 |
| 14 | 统一错误处理 | 自定义异常类 | ⏳ 留待 v1.0 |

### P12 - 测试扩展

| # | 任务 | 说明 | 状态 |
|---|------|------|------|
| 15 | 添加 `information_theory` 测试 | 覆盖熵、互信息、KL 散度 | ✅ 完成 |
| 16 | 添加哈密顿路径测试 | 验证图论扩展 | ✅ 完成 |

---

## 实施详情

### 1. ✅ 新建 `information_theory.py`

实现信息论基础函数：
```python
from typing import List
import math

def entropy(prob_dist: List[float]) -> float:
    """Shannon entropy H(X) = -Σ p_i log2(p_i)."""
    if not prob_dist:
        return 0.0
    total = sum(prob_dist)
    if abs(total - 1.0) > 1e-10:
        prob_dist = [p / total for p in prob_dist]
    h = 0.0
    for p in prob_dist:
        if p > 0:
            h -= p * math.log2(p)
    return h

def mutual_information(joint_dist: List[List[float]], x_marginal: List[float], y_marginal: List[float]) -> float:
    """Mutual information I(X;Y) = Σ p_xy log2(p_xy/(p_x p_y))."""
    mi = 0.0
    for i in range(len(joint_dist)):
        for j in range(len(joint_dist[0])):
            p_xy = joint_dist[i][j]
            if p_xy > 0:
                p_x, p_y = x_marginal[i], y_marginal[j]
                if p_x > 0 and p_y > 0:
                    mi += p_xy * math.log2(p_xy / (p_x * p_y))
    return mi

def kl_divergence(p: List[float], q: List[float]) -> float:
    """KL divergence D_KL(P||Q) = Σ p_i log2(p_i/q_i)."""
    if len(p) != len(q):
        raise ValueError("p and q must have same length")
    kl = 0.0
    for p_i, q_i in zip(p, q):
        if p_i > 0:
            if q_i <= 0:
                return float('inf')
            kl += p_i * math.log2(p_i / q_i)
    return kl
```

### 2. ✅ 扩展 `graph_theory.py`: 哈密顿路径

```python
def has_hamiltonian_path(g: Graph) -> bool:
    """Check Hamiltonian path existence (heuristic using Dirac's condition)."""
    n = len(g.vertices)
    if n < 2:
        return True
    # Dirac's theorem: deg(v) ≥ n/2 for all v ⇒ Hamiltonian cycle exists
    for v in g.vertices:
        if g.degree(v) < n / 2:
            return False
    return True  # Sufficient condition for cycle, hence path
```

### 3. ✅ 导出新模块到 `__init__.py`

```python
from lean4py.information_theory import (
    entropy, mutual_information, kl_divergence,
)
```

---

## 变更范围

| 类型 | 文件 |
|------|------|
| **新增** | `lean4py/information_theory.py`, `lean4py/_doc/v0.9.md`, `tests/test_information_theory.py` |
| **修改** | `lean4py/graph_theory.py`, `lean4py/__init__.py` |
| **测试数** | 424 → ~445 |
| **版本** | `0.8.0` → `0.9.0` |

---

## 验证命令

```bash
# 运行所有测试
pytest tests/ -v
# 预期: ~445 passed

# 检查信息论模块导出
python -c "from lean4py import entropy, mutual_information, kl_divergence; print('OK')"

# 检查哈密顿路径函数
python -c "from lean4py.graph_theory import has_hamiltonian_path; print('OK')"

# 检查版本号
python -c "import lean4py; print(lean4py.__version__)"
# 输出: 0.9.0
```

---

## 总结

v0.9 完成核心新模块和算法扩展：
1. ✅ 新增 `information_theory.py` 模块（熵、互信息、KL 散度）
2. ✅ 扩展 `graph_theory.py` 新增 `has_hamiltonian_path()`
3. ✅ 导出新模块到 `__init__.py`
4. ✅ 新增测试覆盖至 ~445 个
5. ✅ 版本更新至 `0.9.0`

**下一步 (v1.0)**:
- 实现 `optimization.py`（梯度下降、线性规划）
- 扩展图论最大流/最小割算法
- 统一错误处理，自定义异常类
- 完善文档和示例
