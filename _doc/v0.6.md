# lean4py v0.6 规划

> 当前版本: v0.5.0 → v0.6.0
> 规划日期: 2026-04-29
> 目标: 完善 P2 项目，扩展核心模块，补齐测试缺口

---

## 现状摘要 (v0.5.0)

- **总计测试**: 294 passed, 0 failed
- **Python 要求**: >=3.10
- **关键成就**: P0 + P1 全部完成，新增 `graph_clique()`，扩展 `eigenvalues()`
- **已知缺口**: `tactics.py` 无测试文件，`algebra.py` 测试偏少

---

## v0.6 目标

### P2 - 必须完成（续 v0.5）

| # | 任务 | 状态 |
|---|------|------|
| 1 | 实现 `sets.symmetric_difference()` | 待做 |
| 2 | 添加 `nat.factorial()` 和 `nat.fibonacci()` | 待做 |
| 3 | 增加边缘测试用例 | 待做 |

### P3 - 高价值扩展（新功能）

| # | 任务 | 说明 |
|---|------|------|
| 4 | `nat.py` 扩展: `nat_gcd()`, `nat_is_prime()`, `nat_even()`, `nat_odd()` | 让自然数模块更完整 |
| 5 | `sets.py` 扩展: `is_disjoint()`, `is_overlapping()` | 补充集合工具函数 |
| 6 | 创建 `tests/test_tactics.py` | 补齐关键测试缺口（当前 0 测试） |
| 7 | 扩展 `tests/test_algebra.py` | 当前仅 89 行，需增加 Ring/Field 测试 |
| 8 | 添加 `linear_algebra.characteristic_polynomial()` | 特征值分析工具 |
| 9 | 添加 `real_analysis.convergence_tests()` | 级数收敛判别法 |
| 10 | 添加 `probability.ExponentialDistribution` | 常用连续分布 |

### P4 - 可选新模块（若时间允许）

| # | 任务 | 说明 |
|---|------|------|
| 11 | 新建 `statistics.py` 模块 | 描述统计: mean, median, variance, std_dev, correlation |
| 12 | `graph_theory.is_eulerian()`, `graph_coloring()` | 图论算法补充 |

---

## 详细实施计划

### 1. `sets.symmetric_difference()`

```python
def symmetric_difference(a: Set, b: Set) -> Set:
    """返回两个集合的对称差 (A \ B) ∪ (B \ A)"""
    return union(difference(a, b), difference(b, a))
```

### 2. `nat.factorial()` 和 `nat.fibonacci()`

```python
def factorial(n: Nat) -> Nat:
    """Return n! (n factorial)."""
    result = 1
    for i in range(2, n.n + 1):
        result *= i
    return Nat(result)

def fibonacci(n: Nat) -> Nat:
    """Return the nth Fibonacci number (0-indexed)."""
    if n.n <= 1:
        return n
    a, b = Nat(0), Nat(1)
    for _ in range(2, n.n + 1):
        a, b = b, Nat(a.n + b.n)
    return b
```

### 3. 边缘测试用例

为所有模块添加边界条件测试：
- `test_sets.py`: 空集合运算、单元素集合、相等集合的差集
- `test_nat.py`: 0 的运算、大数运算、负数输入（应报错）
- `test_graph_theory.py`: 空图、单节点图、完全图的 clique

### 4. `nat.py` 扩展函数

```python
def nat_gcd(a: Nat, b: Nat) -> Nat:
    """Compute GCD of two natural numbers using Euclidean algorithm."""
    a_val, b_val = a.n, b.n
    while b_val != 0:
        a_val, b_val = b_val, a_val % b_val
    return Nat(a_val)

def nat_is_prime(n: Nat) -> bool:
    """Check if n is prime."""
    v = n.n
    if v < 2:
        return False
    if v == 2:
        return True
    if v % 2 == 0:
        return False
    for i in range(3, int(v ** 0.5) + 1, 2):
        if v % i == 0:
            return False
    return True

def nat_even(n: Nat) -> bool:
    """Check if n is even."""
    return n.n % 2 == 0

def nat_odd(n: Nat) -> bool:
    """Check if n is odd."""
    return n.n % 2 == 1
```

### 5. `sets.py` 工具函数

```python
def is_disjoint(s1: Set, s2: Set) -> bool:
    """Check if s1 and s2 have no elements in common."""
    return len(intersection(s1, s2)._elems) == 0

def is_overlapping(s1: Set, s2: Set) -> bool:
    """Check if s1 and s2 have at least one common element."""
    return len(intersection(s1, s2)._elems) > 0
```

### 6. 创建 `tests/test_tactics.py`（关键缺口）

当前 `tactics.py` 有 151 行代码，但**没有任何测试**。

**测试内容**:
- Tactic 类的基本操作
- 所有 tactic 函数的创建和验证
- TacticProof 的执行流程
- TacticState 的状态管理
- intros, cases, split, by, sorry, calc 等具体 tactics

**预计测试数**: 25-30 个

### 7. 扩展 `tests/test_algebra.py`

当前仅 89 行测试，需增加：
- Ring 和 Field 的完整测试
- 更多代数结构的属性测试
- 边界情况测试

### 8. `linear_algebra.characteristic_polynomial()`

```python
def characteristic_polynomial(M: Matrix) -> List[float]:
    """Compute the characteristic polynomial det(M - λI) coefficients.
    
    Returns coefficients [a_n, a_{n-1}, ..., a_0] for polynomial a_n*λ^n + ...
    For 2x2, returns [1, -trace(M), det(M)]
    """
    if M.rows != M.cols:
        raise ValueError("Square matrix required")
    n = M.rows
    if n == 1:
        return [1.0, -M.data[0][0]]
    if n == 2:
        trace_val = sum(M.data[i][i] for i in range(2))
        det_val = det(M)
        return [1.0, -trace_val, det_val]
    # For larger matrices, use numpy if available
    try:
        import numpy as np
        eigvals = np.linalg.eigvals(np.array(M.data))
        # Build polynomial from roots
        coeffs = np.poly(eigvals)
        return list(coeffs)
    except ImportError:
        return []
```

### 9. `real_analysis` 收敛判别法

```python
def ratio_test(a_n: List[float]) -> str:
    """Ratio test for series convergence.
    
    Returns: "converges", "diverges", or "inconclusive"
    """
    if len(a_n) < 2:
        return "inconclusive"
    ratios = [abs(a_n[i+1] / a_n[i]) for i in range(len(a_n)-1) if a_n[i] != 0]
    if not ratios:
        return "inconclusive"
    limit = sum(ratios) / len(ratios)  # Simple average approximation
    if limit < 1:
        return "converges"
    elif limit > 1:
        return "diverges"
    return "inconclusive"

def root_test(a_n: List[float]) -> str:
    """Root test for series convergence."""
    if not a_n:
        return "inconclusive"
    roots = [abs(x) ** (1/(i+1)) for i, x in enumerate(a_n) if x != 0]
    if not roots:
        return "inconclusive"
    lim_sup = max(roots)
    if lim_sup < 1:
        return "converges"
    elif lim_sup > 1:
        return "diverges"
    return "inconclusive"
```

### 10. `probability.ExponentialDistribution`

```python
class ExponentialDistribution(Distribution):
    def __init__(self, lam: float):
        self.lam = lam
    
    def __repr__(self):
        return f"Exponential({self.lam})"
    
    def pdf(self, x: float) -> float:
        if x < 0:
            return 0.0
        return self.lam * math.exp(-self.lam * x)
    
    def cdf(self, x: float) -> float:
        if x < 0:
            return 0.0
        return 1 - math.exp(-self.lam * x)
    
    def mean(self) -> float:
        return 1.0 / self.lam
    
    def variance(self) -> float:
        return 1.0 / (self.lam ** 2)
    
    def sample(self, n: int = 1) -> List[float]:
        return [random.expovariate(self.lam) for _ in range(n)]
```

### 11. 新建 `statistics.py` 模块（可选）

```python
def mean(data: List[float]) -> float:
    """Arithmetic mean."""
    return sum(data) / len(data) if data else 0.0

def median(data: List[float]) -> float:
    """Median value."""
    s = sorted(data)
    n = len(s)
    if n == 0:
        return 0.0
    if n % 2 == 1:
        return s[n // 2]
    return (s[n // 2 - 1] + s[n // 2]) / 2

def variance(data: List[float]) -> float:
    """Sample variance."""
    if len(data) < 2:
        return 0.0
    m = mean(data)
    return sum((x - m) ** 2 for x in data) / (len(data) - 1)

def std_dev(data: List[float]) -> float:
    """Standard deviation."""
    return math.sqrt(variance(data))

def pearson_correlation(x: List[float], y: List[float]) -> float:
    """Pearson correlation coefficient."""
    if len(x) != len(y) or len(x) < 2:
        return 0.0
    n = len(x)
    sum_x, sum_y = sum(x), sum(y)
    sum_xy = sum(x[i] * y[i] for i in range(n))
    sum_x2 = sum(xi ** 2 for xi in x)
    sum_y2 = sum(yi ** 2 for yi in y)
    numerator = n * sum_xy - sum_x * sum_y
    denominator = math.sqrt((n * sum_x2 - sum_x**2) * (n * sum_y2 - sum_y**2))
    return numerator / denominator if denominator != 0 else 0.0
```

---

## 预计变更范围

| 类型 | 文件 |
|------|------|
| **新增** | `tests/test_tactics.py`, `lean4py/_doc/v0.6.md`, (可选) `lean4py/statistics.py` |
| **修改** | `lean4py/sets.py`, `lean4py/nat.py`, `lean4py/linear_algebra.py`, `lean4py/real_analysis.py`, `lean4py/probability.py`, `lean4py/__init__.py`, `tests/test_nat.py`, `tests/test_sets.py`, `tests/test_algebra.py`, `tests/test_graph_theory.py` |
| **测试数** | 294 → ~350 |
| **版本** | `0.5.0` → `0.6.0` |

---

## 实施顺序

1. P2 #1: 实现 `sets.symmetric_difference()` + 测试
2. P2 #2: 实现 `nat.factorial()` 和 `nat.fibonacci()` + 测试
3. P2 #3: 增加边缘测试用例
4. P3 #4: 扩展 `nat.py` 函数 + 测试
5. P3 #5: 扩展 `sets.py` 函数 + 测试
6. P3 #6: **创建 `tests/test_tactics.py`**（关键缺口）
7. P3 #7: 扩展 `tests/test_algebra.py`
8. P3 #8: 添加 `characteristic_polynomial()` + 测试
9. P3 #9: 添加收敛判别法 + 测试
10. P3 #10: 添加 `ExponentialDistribution` + 测试
11. (可选) P4 #11: 新建 `statistics.py` 模块
12. 运行完整测试套件验证
13. 更新版本号至 `0.6.0`

---

## 验证命令

```bash
# 安装开发版本
pip install -e ".[dev,probability]"

# 运行所有测试
pytest tests/ -v
# 预期: ~350 passed

# 检查新增模块导入
python -c "from lean4py import symmetric_difference, factorial, fibonacci; print('OK')"

# 运行示例验证
python examples/
```

---

## 成功标准

- [ ] 所有 P2 项目完成（symmetric_difference, factorial, fibonacci, 边缘测试）
- [ ] `test_tactics.py` 创建并包含 ≥25 个测试
- [ ] `test_algebra.py` 扩展至 ≥150 行
- [ ] 新增至少 3 个 `nat.py` 函数（gcd, is_prime, even/odd）
- [ ] 新增至少 2 个 `sets.py` 函数（is_disjoint, is_overlapping）
- [ ] 总计测试数 ≥ 340
- [ ] 所有测试通过
- [ ] 版本号更新至 `0.6.0`
